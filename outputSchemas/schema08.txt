ex:JS_id a sh:NodeShape;
  sh:targetClass: JS_id;
  sh:property [
    sh:path version;
    sh:datatype string;
  ];
  sh:property [
    sh:path allowPrerelease;
    sh:datatype boolean;
  ];
  sh:property [
    sh:path rollForward;
    sh:datatype string;
    sh:in patch,feature,minor,major,latestPatch,latestFeature,latestMinor,latestMajor,disable;
  ];
