
ex:subschema_1_Shape a sh:NodeShape;
  sh:targetClass :subschema_1;
  sh:property [
    sh:path ex:fruits;
    sh:node dash:ListShape;
    sh:property [
      sh:path ([sh:zeroOrMorePath redf:rest] ref:first);
      sh:datatype string;
    ];
  ];
  sh:property [
    sh:path ex:vegetables;
    sh:node dash:ListShape;
    sh:property [
      sh:path ([sh:zeroOrMorePath redf:rest] ref:first);
    ];
  ];

ex:subschema_2_Shape a sh:NodeShape;
  sh:targetClass :subschema_2;
  sh:property [
    sh:path ex:fruitas;
    sh:node dash:ListShape;
    sh:property [
      sh:path ([sh:zeroOrMorePath redf:rest] ref:first);
      sh:datatype string;
    ];
  ];
  sh:property [
    sh:path ex:vegetables;
    sh:node dash:ListShape;
    sh:property [
      sh:path ([sh:zeroOrMorePath redf:rest] ref:first);
    ];
  ];
  sh:property [
    sh:path alooou;
    sh:node alooou_Shape;
  ];

ex:JS_id a sh:NodeShape;
  sh:targetClass :JS_id;

ex:alooou_Shape a sh:NodeShape;
  sh:targetClass :alooou;
  sh:property [
    sh:path aqui;
    sh:datatype string;
  ];
