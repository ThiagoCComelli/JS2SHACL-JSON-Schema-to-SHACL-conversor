ex:JS_id a sh:NodeShape;
  sh:targetClass: JS_id;
========
ex:subschema_1_Shape a sh:NodeShape;
  sh:targetClass ex:subschema_1;
  sh:property [
    sh:path ex:fruits;
    sh:node dash:ListShape;
    sh:property [
      sh:path ([sh:zeroOrMorePath redf:rest] ref:first);
      sh:datatype string;
    ];
  ];
  sh:property [
    sh:path ex:vegetables;
    sh:node dash:ListShape;
    sh:property [
      sh:path ([sh:zeroOrMorePath redf:rest] ref:first);
    ];
  ];

ex:uobjeto0_Shape a sh:NodeShape;
  sh:targetClass ex:uobjeto0;
  sh:property [
    sh:path ex:fruits1;
    sh:node dash:ListShape;
    sh:property [
      sh:path ([sh:zeroOrMorePath redf:rest] ref:first);
      sh:datatype string;
    ];
  ];
  sh:property [
    sh:path ex:vegetables1;
    sh:node dash:ListShape;
    sh:property [
      sh:path ([sh:zeroOrMorePath redf:rest] ref:first);
    ];
  ];
  sh:property [
    sh:path zerzero;
    sh:datatype string;
  ];

ex:subschema_2_Shape a sh:NodeShape;
  sh:targetClass ex:subschema_2;
ex:uobjeto0_Shape a sh:NodeShape;
  sh:targetClass ex:uobjeto0;
  sh:property [
    sh:path ex:fruits1;
    sh:node dash:ListShape;
    sh:property [
      sh:path ([sh:zeroOrMorePath redf:rest] ref:first);
      sh:datatype string;
    ];
  ];
  sh:property [
    sh:path ex:vegetables1;
    sh:node dash:ListShape;
    sh:property [
      sh:path ([sh:zeroOrMorePath redf:rest] ref:first);
    ];
  ];
  sh:property [
    sh:path zerzero;
    sh:datatype string;
  ];
